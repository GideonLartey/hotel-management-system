<UserControl x:Class="LantelHotelApp.Views.CheckInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1a1a2e" Offset="0"/><GradientStop Color="#0f3460" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <StackPanel Margin="30,20">
            <Button Content="← Back to Dashboard" Click="Back_Click" HorizontalAlignment="Left" Foreground="#AAFFFFFF" FontSize="12" Cursor="Hand" Margin="0,0,0,15">
                <Button.Template><ControlTemplate TargetType="Button"><TextBlock Text="{TemplateBinding Content}" Foreground="#AAFFFFFF" FontSize="12"/></ControlTemplate></Button.Template>
            </Button>
            <TextBlock Text="✅ Guest Check-In" FontSize="24" FontWeight="Bold" Foreground="#e6a817" Margin="0,0,0,15"/>
            <Border Background="#33000000" CornerRadius="12" Padding="20" BorderBrush="#44FFFFFF" BorderThickness="1" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Booking Reference:" Foreground="#CCFFFFFF" FontSize="13" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox x:Name="TxtRef" Width="180" Height="35" FontSize="13" Background="#22FFFFFF" Foreground="White" BorderBrush="#44FFFFFF" Padding="8,5" CaretBrush="White"/>
                    <Button Content="Pay &amp; Check In" Click="CheckIn_Click" Margin="15,0,0,0" Height="35" Foreground="White" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="b" CornerRadius="6" Background="#88e6a817" Padding="15,5">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="b" Property="Background" Value="#AAe6a817"/></Trigger></ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Border>
            <TextBlock x:Name="TxtStatus" Text="" FontSize="13" TextWrapping="Wrap" Margin="0,10,0,0" Visibility="Collapsed"/>
            <TextBlock Text="Bookings Awaiting Check-In" Foreground="#CCFFFFFF" FontSize="14" FontWeight="SemiBold" Margin="0,15,0,10"/>
            <DataGrid x:Name="DgPending" AutoGenerateColumns="False" IsReadOnly="True"
                      Background="Transparent" Foreground="White" BorderBrush="#44FFFFFF"
                      RowBackground="#22000000" AlternatingRowBackground="#33000000"
                      GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#22FFFFFF" HeadersVisibility="Column" MaxHeight="350">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#44000000"/><Setter Property="Foreground" Value="#e6a817"/>
                        <Setter Property="Padding" Value="10,6"/><Setter Property="FontWeight" Value="SemiBold"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Ref" Binding="{Binding BookingReference}" Width="100"/>
                    <DataGridTextColumn Header="Guest" Binding="{Binding Guest.Name}" Width="130"/>
                    <DataGridTextColumn Header="Room" Binding="{Binding Room.RoomNumber}" Width="60"/>
                    <DataGridTextColumn Header="Check-In" Binding="{Binding CheckInDate, StringFormat=dd/MM/yyyy}" Width="90"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat=N2}" Width="80"/>
                    <DataGridCheckBoxColumn Header="Paid" Binding="{Binding IsPaymentCompleted}" Width="50"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
