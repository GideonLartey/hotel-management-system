<UserControl x:Class="LantelHotelApp.Views.RoomManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.Background><LinearGradientBrush StartPoint="0,0" EndPoint="1,1"><GradientStop Color="#1a1a2e" Offset="0"/><GradientStop Color="#0f3460" Offset="1"/></LinearGradientBrush></Grid.Background>
        <StackPanel Margin="30,20">
            <Button Content="← Back to Dashboard" Click="Back_Click" HorizontalAlignment="Left" Foreground="#AAFFFFFF" FontSize="12" Cursor="Hand" Margin="0,0,0,15">
                <Button.Template><ControlTemplate TargetType="Button"><TextBlock Text="{TemplateBinding Content}" Foreground="#AAFFFFFF" FontSize="12"/></ControlTemplate></Button.Template>
            </Button>
            <TextBlock Text="🏠 Room Management" FontSize="24" FontWeight="Bold" Foreground="#e6a817" Margin="0,0,0,15"/>
            <UniformGrid Rows="1" Columns="3" Margin="0,0,0,15">
                <Border Background="#33000000" CornerRadius="10" Padding="15" Margin="0,0,8,0" BorderBrush="#44FFFFFF" BorderThickness="1">
                    <StackPanel><TextBlock x:Name="TxtTotal" Text="0" FontSize="28" FontWeight="Bold" Foreground="#60a5fa"/><TextBlock Text="Total Rooms" Foreground="#AAFFFFFF" FontSize="11"/></StackPanel>
                </Border>
                <Border Background="#33000000" CornerRadius="10" Padding="15" Margin="4,0" BorderBrush="#44FFFFFF" BorderThickness="1">
                    <StackPanel><TextBlock x:Name="TxtAvail" Text="0" FontSize="28" FontWeight="Bold" Foreground="#4ade80"/><TextBlock Text="Available" Foreground="#AAFFFFFF" FontSize="11"/></StackPanel>
                </Border>
                <Border Background="#33000000" CornerRadius="10" Padding="15" Margin="8,0,0,0" BorderBrush="#44FFFFFF" BorderThickness="1">
                    <StackPanel><TextBlock x:Name="TxtOccup" Text="0" FontSize="28" FontWeight="Bold" Foreground="#f87171"/><TextBlock Text="Occupied" Foreground="#AAFFFFFF" FontSize="11"/></StackPanel>
                </Border>
            </UniformGrid>
            <DataGrid x:Name="DgRooms" AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" Foreground="White" BorderBrush="#44FFFFFF" RowBackground="#22000000" AlternatingRowBackground="#33000000" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#22FFFFFF" HeadersVisibility="Column" MaxHeight="400">
                <DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#44000000"/><Setter Property="Foreground" Value="#e6a817"/><Setter Property="Padding" Value="10,6"/><Setter Property="FontWeight" Value="SemiBold"/></Style></DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Room #" Binding="{Binding RoomNumber}" Width="80"/>
                    <DataGridTextColumn Header="Floor" Binding="{Binding FloorNumber}" Width="60"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding RoomType}" Width="130"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="130"/>
                    <DataGridTextColumn Header="Last Cleaned" Binding="{Binding LastCleaned, StringFormat=dd/MM HH:mm}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
